import { ReactNode } from "react";
import "./Main.css";

export const Main = ({ children }: { children: ReactNode }) => {
  return <div>{children}</div>;
};
//pensar que utilidad le puedoi dar a este componente
